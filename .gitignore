# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Virtual env ---
.venv/
venv/

# --- Data (large; download via scripts/download_data.py) ---
data/
!data/.gitkeep

# --- Model artifacts (large) ---
models/*.pkl
models/*.pt
models/*.cbm
models/*.txt
models/*.joblib
# …but keep the small LightGBM model + schema so the Gradio demo runs standalone
!models/model_lgbm.txt
!models/feature_schema.joblib
!models/.gitkeep

# --- MLflow ---
mlruns/
mlartifacts/
mlflow.db

# --- CatBoost training scratch ---
catboost_info/

# --- Generated reports / figures (kept — small, show results on GitHub) ---
# (figures and CSVs under reports/ ARE committed)
artifacts/*.parquet
# …keep the small demo sample so the Gradio app has applicants to score
!artifacts/demo_applicants.parquet

# --- Secrets ---
.env
*.key

# --- OS ---
.DS_Store
Thumbs.db
