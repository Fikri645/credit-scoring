# Pin only where it matters; let pip resolve the rest to avoid conflicts.
# torch is installed separately (CUDA build) — see README.

# --- Core data stack ---
polars>=1.17,<2.0
pandas>=2.1,<2.3
numpy>=1.26,<2.0
pyarrow>=16.0
scikit-learn>=1.4,<1.6
scipy>=1.11

# --- Gradient boosting ---
lightgbm>=4.3,<4.6
catboost>=1.2,<1.3
xgboost>=2.0

# --- Tabular deep learning (FT-Transformer) ---
rtdl-revisiting-models>=0.0.2

# --- Explainability ---
shap>=0.44
dice-ml>=0.11

# --- Fairness ---
fairlearn>=0.10

# --- Survival analysis ---
lifelines>=0.28

# --- Experiment tracking ---
mlflow>=2.14

# --- Serving ---
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
gradio>=4.40

# --- Plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- Testing / lint ---
pytest>=8.0
pytest-cov>=5.0
flake8>=7.0

# --- Data download ---
# Pinned to the last 1.x release: kaggle 2.x switched to OAuth and dropped
# classic kaggle.json (username/key) auth used by this project.
kaggle==1.6.17

# --- Misc ---
joblib>=1.3
tqdm>=4.66
